#include <stdio.h>
#include <SDL2/SDL.h>

int main(int argc, char** args) {
	printf("Testando abertura de janela\n");
	
	// Pointers to our window and surface
	SDL_Surface* winSurface = NULL;
	

	SDL_Init(SDL_INIT_EVERYTHING);

	return 0;
}

